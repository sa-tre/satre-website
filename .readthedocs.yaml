# https://docs.readthedocs.com/platform/stable/build-customization.html

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3"
    nodejs: "22"

  jobs:
    install:
      - ./ci/download-hugo.sh
    build:
      html:
        - ./ci/readthedocs-build.sh
